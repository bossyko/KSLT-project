-- ============================================================
-- Санжар Султанов: имя и фамилия местами
-- ============================================================
--
-- Карточка `sanzhar-sultanov` записана как «Султан Санжаров» — имя ушло в
-- фамилию, а фамилия в имя. Человек тот же: он играет ТБШ Challengers 2026 и
-- стоит вторым в паре с Элнуром Омурзаковым в дружеском парном Futures.
-- Второй карточки на него нет, сливать нечего — просто правим написание.
--
-- Парный NTRP пока не ставлю: числа от Costa ещё нет. Когда скажет — впишем
-- строкой ниже.
--
-- Запускать можно повторно.

BEGIN;

UPDATE public.players
   SET name = 'Санжар Султанов'
 WHERE id = 'sanzhar-sultanov';

-- Когда будет парный рейтинг, раскомментируй и подставь число:
-- UPDATE public.players SET ntrp_doubles = 4.5 WHERE id = 'sanzhar-sultanov';

COMMIT;

-- ---- Проверка ----

SELECT id, name,
       ntrp_singles AS одиночный, ntrp_doubles AS парный,
       category_id AS разряд, points AS очки
  FROM public.players
 WHERE id = 'sanzhar-sultanov';
