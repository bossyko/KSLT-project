-- ============================================================
-- Микст FUTURES: очистить историю замен — ПРАВКА
-- ============================================================
--
-- История замен живёт отдельно от состава, в registration_changes, и сброс
-- жеребьёвки её не трогает: она про действия менеджера, а не про то, кто с
-- кем играет. Обычно это правильно — журнал должен переживать пересборку
-- сетки.
--
-- Но здесь турнир собирается заново, а состав приведён к выгрузке формы.
-- Записи остались от тестовой жеребьёвки и показывают замены, которых в
-- турнире больше нет: «Урмат Алабаев → Адиль Валимамедов», хотя Урмат как
-- раз вернулся на своё место.
--
-- Чистим журнал только этого турнира. Остальные не трогаем.
--
-- Запускать можно повторно.

BEGIN;

DELETE FROM public.registration_changes
 WHERE tournament_id = 'c0a30bae-30ee-4a38-a26a-4c6b339bd695';

COMMIT;

-- ---- Проверка ----

SELECT count(*) AS осталось_записей
  FROM public.registration_changes
 WHERE tournament_id = 'c0a30bae-30ee-4a38-a26a-4c6b339bd695';
