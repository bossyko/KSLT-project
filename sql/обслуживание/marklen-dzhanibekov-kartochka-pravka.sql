-- ============================================================
-- Карточка Марклену Джанибекову — ПРАВКА
-- ============================================================
--
-- Заводим карточку, чтобы его можно было поставить на замену в турнире.
-- Заявка без карточки оставляет в матчах пустую сторону — счёт туда не
-- вписать.
--
-- Что известно: мужчина, Кыргызстан. Рейтинга и NTRP нет — поля оставляем
-- пустыми, проставят потом.
--
-- Категория futures — в неё он и попадает по замене.
--
-- Гостем не помечаем: в рейтинге гости не показываются (их отсеивает
-- выборка рейтинга), а турнир рейтинговый, и очки за него должны лечь в
-- таблицу. Членство оставляем выключенным — его ставит клуб отдельно.
--
-- Однофамилица в базе есть (Нуржамал Джанибекова) — это другой человек.
--
-- Запускать можно повторно.

BEGIN;

INSERT INTO public.players (id, name, country, category_id, gender,
                            is_member, is_guest, has_account)
VALUES ('marklen-dzhanibekov', 'Марклен Джанибеков', '🇰🇬', 'futures', 'men',
        false, false, false)
ON CONFLICT (id) DO NOTHING;

COMMIT;

-- ---- Проверка ----

SELECT id, name, country, category_id, gender,
       ntrp_singles, points, is_member, is_guest, has_account
  FROM public.players
 WHERE id = 'marklen-dzhanibekov';
