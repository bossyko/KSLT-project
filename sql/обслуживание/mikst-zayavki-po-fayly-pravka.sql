-- ============================================================
-- Микст FUTURES: заявки строго по выгрузке формы — ПРАВКА
-- ============================================================
--
-- Приводим состав к файлу клуба. Вчерашние замены из листа ожидания
-- откатываются: они делались на тестовой жеребьёвке, которую сносим.
--
-- Что меняется (остальные 19 пар совпадают с файлом и не трогаются):
--
--   Айгерим Алижанова    — без напарника: в файле «определяется»
--   Нуриза Момунова      — напарник Урмат Алабаев вместо Адиля Валимамедова
--   Виктория Хан         — без напарника (был Евгений Горских)
--   Бекжан Кылычбеков    — без напарника (была Альбина Сулайманова)
--   Леонид Цой           — без напарника (была Каракоз Болотбекова)
--   Табалдиева Айканыш   — без напарника (был Азат Базаркулов), в очередь
--   Нурсултан + Аажара   — в очередь, как в файле
--   Каракоз + Жакшылык   — в очередь (были сняты)
--   Азат Базаркулов      — в очередь (был снят)
--   Евгений Горских      — в очередь (был снят)
--   Константин Хан       — заявка удаляется: тестовая, в файле её нет
--
-- Итог: 22 заявки в составе (четыре из них без напарника) и 6 в очереди.
--
-- Порядок важен: сперва освобождаем людей от чужих пар, потом возвращаем
-- их собственные заявки в очередь. Иначе правило «один игрок — одна пара»
-- остановит на середине.
--
-- Правило на время правки выключаем: мы не сводим людей, а разводим, но
-- проверка перебирает соседние заявки и спотыкается на промежуточном
-- состоянии.
--
-- Жеребьёвку перед этим снести файлом mikst-sbros-do-zayavok-pravka.sql:
-- состав меняется, и старые матчи к нему отношения не имеют.
--
-- Запускать можно повторно.

BEGIN;

ALTER TABLE public.tournament_registrations DISABLE TRIGGER USER;

-- ---- 1. Освобождаем людей от чужих пар ----

UPDATE public.tournament_registrations
   SET partner_id = NULL, guest_confirmed = false
 WHERE id IN (
    'b7dcaa60-a2e3-426f-8b55-cf02fe18e438',  -- Виктория Хан — Евгений Горских
    'caf05bea-81d6-4d23-9da2-5c937b8f5814',  -- Бекжан Кылычбеков — Альбина Сулайманова
    'c9d9dfb8-17f2-441e-a858-fa108e90ad1c',  -- Леонид Цой — Каракоз Болотбекова
    '6767e89a-9750-4a6f-a345-5f02b886b8fb',  -- Табалдиева Айканыш — Азат Базаркулов
    '1a59d095-b25c-466b-a964-95c8492cb072'   -- Айгерим Алижанова — Евгений Осипов
 );

-- ---- 2. Нуризе возвращаем напарника из файла ----

UPDATE public.tournament_registrations
   SET partner_id = 'urmat-alabaev'
 WHERE id = '905f2c4f-8832-45a9-900e-4d53498c2eb2';

-- ---- 3. Лист ожидания — как в файле ----

UPDATE public.tournament_registrations
   SET status = 'waitlist', withdrawn_at = NULL
 WHERE id IN (
    'd5fa1211-859b-4b1d-b03e-8a5015c48d2f',  -- Нурсултан + Аажара
    '6767e89a-9750-4a6f-a345-5f02b886b8fb',  -- Табалдиева Айканыш
    '157e9f20-4326-46c0-8af1-1f17eff1ef46',  -- Каракоз + Жакшылык
    '5a404de1-45c3-4f64-b8a2-c8840c114af3',  -- Азат Базаркулов
    'b190dd01-5409-44bd-98af-13cc18ef87fc',  -- Евгений Горских
    '7f70d191-353d-4da7-8964-a0dbb6bc0f67'   -- Ахмеджан Аджуев (уже в очереди)
 );

-- ---- 4. Тестовая заявка ----
--
-- Матчей нет (жеребьёвка снесена), поэтому удаляем целиком: место в сетке
-- за ней не числится

DELETE FROM public.tournament_registrations
 WHERE id = '22b99c3d-337d-4835-bf36-c33f7608d7ff';

ALTER TABLE public.tournament_registrations ENABLE TRIGGER USER;

COMMIT;
