-- ============================================================
-- Микст FUTURES: снять плей-офф, чтобы собрать заново — ПРАВКА
-- ============================================================
--
-- Расстановка задаётся в момент сборки сетки, поэтому новое правило —
-- победители доп. матчей идут к верхним сеяным, а прошедшие без игры к
-- следующим — подействует только на заново собранный плей-офф.
--
-- Удаляем матчи вне групп: сетку и доп. матчи. Результаты групп остаются
-- нетронутыми — у них group_number заполнен.
--
-- После запуска обновить админку: на месте сетки появится кнопка «Формат
-- плей-офф», ею и собрать заново.
--
-- Запускать можно повторно.

BEGIN;

DELETE FROM public.matches
 WHERE tournament_id = 'c0a30bae-30ee-4a38-a26a-4c6b339bd695'
   AND group_number IS NULL;

COMMIT;
